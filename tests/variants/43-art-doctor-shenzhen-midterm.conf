# 深圳博士中期：三个选项选中，不再有专门的类。
# v3.2a 之前这一档有个 hithesisartplus 空壳，42 号变体比的就是「空壳与三个选项
# 输出逐字节相同」；空壳在 v3.2a 移除，那一格跟着删，改由 testfiles/42 钉住
# 旧类名会报错。
BASE=examples/demo
OPTIONS=fontset=fandol,degree-level=doctor,stage=interim,campus=shenzhen
CLS=hithesis
ENTRY=report.tex
