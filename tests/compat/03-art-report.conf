# 报告类没有新旧版面之分（hit-report.cls 里就没有那个标志位），所以当前侧
# 不传 v2-layout，比的是「同一份老内容，老类对新类」。
BASE=examples/hitart/reports
ENTRY=report.tex
OPTIONS=fontset=fandol,toc=true,type=doctor,stage=opening,campus=shenzhen
CLS=hithesisart
# 当前侧：三个旧类名 v3.2a 已移除，统一从 hithesis 进，stage 选交付物。
# 参照侧那一行留着 hithesisart，它编的是 v3.1e 的树。
OPTIONS_NEW=fontset=fandol,toc=true,degree-level=doctor,stage=proposal,campus=shenzhen
CLS_NEW=hithesis
