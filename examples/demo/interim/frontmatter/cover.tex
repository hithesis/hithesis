% !Mode:: "TeX:UTF-8"

\hitsetup{
  info={
    %******************************
    % 注意：
    %   1. 配置里面不要出现空行
    %   2. 不需要的配置信息可以删除
    %   3. 中文字段收在 zh={} 里，字段名不带后缀。
    %      这是推荐写法；带后缀的 title-zh={...} 也认，两种等价
    %******************************
    class-id={9527},
    zh={
      %% 花括号分组给题目栏那两条固定线分行
      % 日期不写就取编译当天。要指定就写 ISO 数字：yyyy、yyyy-mm、yyyy-mm-dd
      % 都认，月日补不补零都行；中文封面出「2026 年 6 月」，英文封面出
      % 「June, 2026」。写成别的样子就原样排出来，日志里会说一句。
      %date={2026-06}
      enroll-date={2020}
    },
  },
}
