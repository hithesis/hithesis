# 旧版 TeX Live 需要额外声明的包
#
# .github/workflows/test.yml 只在 TeX Live < 2024 的矩阵条目上追加这个文件。
#
# 什么时候往这里加：某个旧版本报「找不到某个 .sty」而新版本正常。一般是因为那个包在
# 旧版里不属于 tl_packages 声明的任何 collection，或者当时还是独立包、后来才被并进去。
#
# 眼下是空的。等 2022/2023 的矩阵条目报错了，照实际报错补。
