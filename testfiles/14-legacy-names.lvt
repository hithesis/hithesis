% 改名之后旧名还能不能用。
% 元信息字段与常量键在 v3.2a 改成了英文 kebab 名。字段在 v3.1e 就是公开接口，
% 旧名保留到 v4.1；常量键的 const= 子族从没发布过，旧名不保留。
%
% 42 变体用的是新名，编出来一切正常也证明不了旧名还活着。这里把三条路各走一遍：
% 分族写法、扁平写法、同名命令。断言的是新字段里的值，不是提示文案——文案改了
% 不该让测试红，值丢了才该。
%
% 常量键那边反过来断言：旧名已经不认了，设了不生效，新名的默认值原样保留。
\input{regression-test}
\documentclass[fontset=fandol,degree-level=doctor,campus=harbin]{hit-thesis}
\hitsetup{info={ctitle={旧名分族写法}}}
\hitsetup{cauthor={旧名扁平写法}}
\makeatletter
\cxueke{旧名命令写法}
\makeatother
\hitsetup{const={cthesisname={旧常量名}}}
\begin{document}
\START
\makeatletter
\TEST{旧字段名三种写法都还能设值}{
  \typeout{title-zh=[\hit@title@flat@zh]}
  \typeout{author-zh=[\hit@author@zh]}
  \typeout{discipline-zh=[\hit@discipline@zh]}
}
\TEST{旧常量名不再生效，默认值保持}{
  \typeout{document-type=[\hit@document@type]}
}
\makeatother
\END
\end{document}
