% 与 08 配套，钉住本科页码相关的三个默认值。
%
% 这里记录一个已确认的问题，供将来处理。src/hithesis-book.dtx 里
% bsfrontpagenumberline 的声明写成嵌套条件，意图是「哈尔滨本科为 true，其余
% 为 false」，但那段条件在 \ProcessKeyvalOptions 之前求值：那时 campus 与 type
% 都还没解析，harbin 与 bachelor 由前面的兜底逻辑置成了真，所以条件恒取第一
% 支，任何校区任何学位下默认值都是 true。实测深圳硕士也是 true。
%
% 该问题自 v3.1d（2025-03-03）就在，不是重构引入的。改掉会让深圳与威海本科的
% 前置部分页码样式发生变化，属于用户可见行为，要单独评估。本文件按现状钉住。
\input{regression-test}
\documentclass[fontset=fandol,type=bachelor,campus=harbin]{hithesisbook}
\begin{document}
\START
\makeatletter
\TEST{哈尔滨本科的条件默认值}{
  \typeout{bsfrontpagenumberline=\ifhit@bsfrontpagenumberline true\else false\fi}
  \typeout{bsmainpagenumberline=\ifhit@bsmainpagenumberline true\else false\fi}
  \typeout{bsheadrule=\ifhit@bsheadrule true\else false\fi}
}
\makeatother
\END
\end{document}
