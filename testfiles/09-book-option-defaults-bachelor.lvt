% 与 08 配套，钉住本科页码相关的三个默认值。哈尔滨本科下
% bsfrontpagenumberline 应为 true，这是它唯一为真的组合。
%
% 该默认值曾经失效：原先把嵌套条件写在选项声明处，而那里在
% \ProcessKeyvalOptions 之前执行，campus 与 type 尚未解析，条件恒取第一支，
% 任何组合下都是 true。v3.2a 改为声明后再在选项后处理里补默认值。
% 反面与显式设置的情形见 12 与 13。
\input{regression-test}
\documentclass[fontset=fandol,degree-level=bachelor,campus=harbin]{hit-thesis}
\begin{document}
\START
\makeatletter
\TEST{哈尔滨本科的条件默认值}{
}
\makeatother
\END
\end{document}
