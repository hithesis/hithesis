% 深圳本科被重定向到哈尔滨版式（提交 e4c8096）。这条重定向只在选项后处理里
% 发生，示例文档编出来看不出“本来该是深圳”。
%
% 断言的是最终生效的校区这个值，不是某个标志位的内部表示。以后 \newif 换成
% \bool 或者标志位改名，要改的只是本文件，基线内容不变。基线不动，它的每一处
% 差异才真的代表行为变了。
\input{regression-test}
\documentclass[fontset=fandol,degree-level=bachelor,campus=shenzhen]{hit-thesis}
\begin{document}
\START
\makeatletter
\TEST{深圳本科的最终校区}{
  \typeout{campus=\hit@if@option@TF{shenzhen}{shenzhen}{\hit@if@option@TF{harbin}{harbin}{\hit@if@option@TF{weihai}{weihai}{none}}}}
}
\makeatother
\END
\end{document}
