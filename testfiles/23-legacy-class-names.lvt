% 三个旧类名在 v3.2a 移除。
%
% v3.2a 之前它们是转发用的空壳（hithesisbook→hit-thesis、hithesisart→hit-report、
% hithesisartplus→hit-report 加三个选项）。现在统一从 hithesis 进、交付物由
% stage 选，空壳没有存在的理由了：留着等于同一件事有两套写法，而改法只是换一个
% 类名，一行的事。
%
% 但不能就这么把文件删掉——那样用户拿到的是 LaTeX 自己的「File `hithesisbook.cls'
% not found」，看不出该改成什么。所以三个名字各留一份只报错的类文件，报完就
% \stop：恰好一条错误，消息里写清改成什么，没有连锁的“未定义控制序列”。
%
% 这里钉的是“报了错”这件事本身，不钉文案——文案改了不该让测试红。
% 一份 lvt 只能写一个 \documentclass，所以三个名字分成三份：本文件试
% hithesisbook，24 试 hithesisart，72 试 hithesisartplus。
\input{regression-test}
\showoutput
\documentclass[fontset=fandol,degree-level=doctor,campus=harbin]{hithesisbook}
