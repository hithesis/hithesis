% 用户显式写了 bsfrontpagenumberline，后处理不得覆盖。
% 这里挑一个默认值本该是 false 的组合（威海本科），显式设成 true。
\input{regression-test}
\documentclass[fontset=fandol,type=bachelor,campus=weihai,bsfrontpagenumberline=true]{hithesisbook}
\begin{document}
\START
\makeatletter
\TEST{显式设置优先于默认值}{
  \typeout{bsfrontpagenumberline=\ifhit@bsfrontpagenumberline true\else false\fi}
}
\makeatother
\END
\end{document}
